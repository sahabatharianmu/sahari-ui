/**
 * Toggle switch for on/off settings — notifications, privacy, availability.
 */
export interface SwitchProps { checked?: boolean; onChange?: (v: boolean) => void; disabled?: boolean; label?: string; }
