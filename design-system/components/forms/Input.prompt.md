Input is the standard single-line text field — profile forms, search, login, booking notes.
```jsx
<Input label="Email" placeholder="you@email.com" hint="We'll never share this" />
```
Pass error to switch the border/hint to the danger color; keep error copy calm and instructive, never alarming.