/**
 * Rounded checkbox for multi-select lists and consent/agreement rows.
 */
export interface CheckboxProps { label?: string; checked?: boolean; onChange?: (v: boolean) => void; disabled?: boolean; }
