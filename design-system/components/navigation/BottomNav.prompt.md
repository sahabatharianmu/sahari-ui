BottomNav is the root mobile navigation — Home, Discover, Bookings, Messages, Profile.
```jsx
<BottomNav items={[{value:"home",label:"Home",icon:<Icon name="home"/>}, ...]} active={tab} onChange={setTab} />
```