A plain 1px hairline rule for separating sections or list rows — never a decorative element.

```jsx
<Divider />
```

Uses `--border-subtle`, the same low-contrast neutral as every other hairline in the system (card borders, input borders). No color, weight, or spacing variants — if you need extra spacing around it, wrap it or pass `style`.
