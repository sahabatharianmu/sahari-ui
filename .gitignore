node_modules
dist
storybook-static
coverage
.DS_Store

# design-sync (claude.ai/design) — local tool state, not project source
.ds-sync/
ds-bundle/
.design-sync/

# Claude Code local session/worktree state
.claude/

# agent skills — local tooling, not project source
.agents/skills/

# raw Claude-generated design-system reference/moodboard — local only, not production source
design-system/
